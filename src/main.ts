class ItemModel {
  constructor(
    name: string,
    yieldPerSecond: number,
    isHighEfficiency: boolean,
    materials: { string: number },
  ) {}
}

class ManufactureModel {
  constructor() {}
}

function calculate(quantity: number) {}

export default calculate;
